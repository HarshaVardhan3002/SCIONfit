"""Deploy front-end: the same harness pointed at real SCION.

If a model trained in ``gym``, checked by ``conformance`` and scored by
``bench`` needs a code change to run here, a seam has been broken.

Empty until M9.
"""
