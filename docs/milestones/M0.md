# M0 — Foundation reset

**Goal.** Restructure the repo into the target layout with zero behaviour change, so that
every later milestone lands in the right place instead of being moved later.

**Depends on.** Nothing. **Blocks.** Everything.
**Size.** Small. One or two sessions.

---

## Deliverables

1. **Package restructure.** `scionfit` becomes `scionarena`, with the existing code moved:
   - `interface.py` → `exposure/contracts.py` (unchanged content)
   - `env/world.py` → `backends/analytical.py` (unchanged for now; rewritten in M1)
   - `probes/`, `report.py`, `runner.py` → `conformance/`
   - `reference/` → `reference/` (top level; used by every front-end)
   - empty packages with docstrings for `core/`, `instrument/`, `bench/`, `gym/`, `agent/`,
     `deploy/`, so the shape is visible from the first commit

2. **Import discipline enforced in CI.** Add `import-linter` with the contract:
   ```
   core  <-  exposure  <-  {conformance, bench, gym, agent, deploy}
   ```
   plus a rule that nothing under `reference/` or any user model imports `scionarena.core`.
   A violation must fail the build, not a review.

3. **Toolchain.** `mypy --strict` on `core/` and `exposure/` (both nearly empty, so it is
   free to adopt now and expensive later). `ruff` format in addition to lint. Pre-commit
   config. CI matrix on 3.11 / 3.12 / 3.13.

4. **Docs skeleton.** `docs/adr/0001-record-architecture-decisions.md`,
   `docs/OPEN_QUESTIONS.md` seeded with Q1–Q6 from HANDOFF §8, `VENDORED.md`,
   `benchmarks/` with an empty `baseline.json`.

5. **Determinism harness.** `core/trace.py` with a `TraceHash` helper and one test
   asserting that the existing conformance run over a fixed seed produces a stable hash.
   This is the tripwire for invariant 4 and it must exist before M1 adds any state.

---

## Acceptance criteria

- [x] All 34 existing tests pass unmodified except for import paths — 47 now, the 13 new
      ones are `tests/test_trace.py`
- [x] `ruff check`, `ruff format --check`, `mypy --strict src/scionarena/core src/scionarena/exposure`, `pytest`, `lint-imports` all green in CI
- [x] `scionarena conformance check reference` reproduces the v0.1 verdict exactly —
      CONFORMANT, mean score 0.98
- [x] `python -m scionarena.conformance.cli compare` still discriminates — matrix output is
      byte-identical to v0.1 (md5 `5e8b6cc4`); `proportional` still FAILs R1 and R3
- [x] The trace hash test passes twice in a row with the same value — and in two separate
      processes under different `PYTHONHASHSEED`, which is the stronger claim
- [x] A deliberately added illegal import (`core` importing `conformance`) fails CI —
      checked by hand: `lint-imports` exits 1 and names the chain

## Must not break

The `PathModel` protocol signatures. The `DECLARED_ABSENT` / `FALSE_CLAIM` semantics. The
cross-model discrimination matrix.

## Notes

Resist improving anything while moving it. A restructure PR that also changes behaviour
cannot be reviewed. If you find a bug during the move, note it in the PR and fix it in a
follow-up.

## Follow-ups found during M0

Neither was fixed here, because both change behaviour and this milestone must not.

1. **`hash(str)` seeds the per-probe RNG.** `conformance/runner.py` seeds each probe with
   `seed ^ (hash(p.probe_id) & 0xFFFF)`, and `hash` on a `str` is salted per process. It
   happens not to move any current verdict or any current report field — verified across
   three `PYTHONHASHSEED` values and pinned by
   `tests/test_trace.py::test_hash_is_stable_across_processes` — but it is a
   nondeterminism source sitting in the substrate's seeding path, and M1 must replace it
   with a stable digest before adding state that depends on it. Doing so will move the
   pinned trace hash; that is expected and the PR must say so.

2. **`backends/` is outside the layers contract.** The v0.1 analytical world imports
   `exposure.contracts`, which is the wrong direction for a backend. M1 rewrites it and it
   joins the contract then. See `docs/adr/0004-m0-package-layout-and-enforced-import-direction.md`.
